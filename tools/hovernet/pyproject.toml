[project]
name = "pathomics-hovernet"
version = "0.1.0"
description = "Isolated HoVer-Net environment for one pathomics ROI patch"
requires-python = ">=3.11,<3.12"
dependencies = [
  "tiatoolbox==2.1.3",
]

[tool.uv]
package = false
